-- BEFORE optimization (slow)
SELECT * FROM Sales WHERE CustomerName LIKE '%Customer1%';

-- Optimization
CREATE NONCLUSTERED INDEX IDX_Sales_CustomerName
ON Sales(CustomerName);

-- AFTER optimization
SELECT * FROM Sales WHERE CustomerName = 'Customer1';

